@{
    ViewBag.Title = "Welcome";
}
<div class="center-text col-md-8 col-md-offset-2">
    <h1 class="cover-heading">Welcome to CSGSI Stat Trakr</h1>
    <p class="lead">CSGSI Stat Trakr is the go to tool for stepping up your Counter Strike game! Dont' get salty, get CSGSI Stat Trakr!</p>
    @if (!User.Identity.IsAuthenticated)
    {
        <p class="lead">
            <a asp-controller="Register" asp-action="Index" class="btn btn-lg btn-default">Register</a>
        </p>
    }
</div>